apiVersion: v2
name: app
description: Helm chart with hierarchical ConfigMap and Secret overrides (default → environment → namespace → application)
type: application
version: 0.1.0
appVersion: "1.0.0"
