<?xml version="1.0" encoding="UTF-8"?>
<server description="DB2 MicroProfile Sample App">

    <featureManager>
        <feature>restfulWS-3.1</feature>
        <feature>jsonb-3.0</feature>
        <feature>jsonp-2.1</feature>
        <feature>cdi-4.0</feature>
        <feature>mpConfig-3.1</feature>
        <feature>mpHealth-4.0</feature>
    </featureManager>

    <httpEndpoint id="defaultHttpEndpoint" httpPort="9080" host="*"/>

    <webApplication location="db2-microprofile-app.war" contextRoot="/">
        <classloader apiTypeVisibility="+third-party"/>
    </webApplication>

    <!-- DB connection defaults (overridden by Conjur init container secrets) -->
    <variable name="db.host" defaultValue="localhost"/>
    <variable name="db.port" defaultValue="50000"/>
    <variable name="db.name" defaultValue="empdb"/>
    <variable name="db.username" defaultValue="not-configured"/>
    <variable name="db.password" defaultValue="not-configured"/>

</server>
