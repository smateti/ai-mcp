# Secrets — never commit these
.env
.secrets/

# Pipeline artifacts (generated during local runs)
app-metadata.json
build.properties
values-final.yaml
deploy-receipt.json
resources-created.json
image-ref.env
