# =============================================================================
# Prod environment overrides
# =============================================================================
replicaCount: 3

resources:
  limits:
    cpu: "1"
    memory: 1Gi
  requests:
    cpu: 250m
    memory: 512Mi

route:
  host: hello.apps.ocp.example.com
  tls:
    termination: reencrypt

config:
  appEnvironment: prod
