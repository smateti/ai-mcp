# Default MicroProfile Config values.
# These are overridden by:
#   1. build.properties (injected by the pipeline's prepare stage)
#   2. Environment variables (set via ConfigMap/Deployment in OpenShift)
#   3. System properties (-D flags)
app.name=hello-service
app.environment=local
app.version=dev
app.description=Sample MicroProfile Hello Service
